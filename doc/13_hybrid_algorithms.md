以下は **フラットな Markdown** です。そのまま
`doc/13_hybrid_algorithms.md` として保存できます。

---

# doc/13_hybrid_algorithms.md

# 第13回 ハイブリッド量子古典アルゴリズム

## はじめに

前回は：

* IBM Quantumへの接続
* backendの選択
* transpileの役割
* 実機実行
* ノイズの影響

を扱いました。

ここまでで：

量子回路を
シミュレータでも
実機でも

実行できるようになりました。

今回は：

**ハイブリッド量子古典アルゴリズム**

という

NISQ時代の中心モデル

を整理します。

---

# なぜハイブリッドが必要なのか？

理想的な量子コンピュータは：

単独で問題を解けます。

例：

Shor
Grover
QFT

しかし：

現在の量子コンピュータは：

ノイズが多い
量子ビットが少ない
回路が浅い

という制約があります。

そのため：

古典計算と組み合わせます。

---

# ハイブリッドアルゴリズムとは何か？

ハイブリッド量子古典アルゴリズムとは：

量子コンピュータ

と

古典コンピュータ

を協調させる計算モデルです。

役割分担：

量子コンピュータ：

状態生成

古典コンピュータ：

最適化

---

# 基本構造

ハイブリッドアルゴリズムは：

次のループで動きます：

1 回路を生成
2 実機で実行
3 測定結果取得
4 古典最適化
5 パラメータ更新
6 繰り返す

これが：

NISQ時代の基本構造

です。

---

# なぜこの方法が有効なのか？

量子コンピュータは：

すべてを計算するのは苦手

ですが：

状態空間探索

は得意です。

古典コンピュータは：

最適化

が得意です。

つまり：

役割分担が自然です。

---

# 代表的なハイブリッドアルゴリズム

代表例：

VQE
QAOA
量子機械学習
変分量子回路

これらはすべて：

同じ構造

を持っています。

---

# 変分量子回路とは何か？

変分量子回路とは：

パラメータ付き量子回路

です。

例：

RY(θ)

のような回転角が

パラメータになります。

つまり：

回路そのものを

調整可能にします。

---

# ハイブリッド計算の流れ（例）

例：

VQEの場合：

1 θを決める
2 回路を実行
3 エネルギー測定
4 θを更新
5 繰り返す

つまり：

最小値探索

です。

---

# Pythonで簡単な例を書く

簡単な例：

```python
from qiskit import QuantumCircuit
from qiskit.quantum_info import Statevector
import numpy as np

def circuit(theta):
    qc = QuantumCircuit(1)
    qc.ry(theta, 0)
    return qc

def expectation(theta):
    state = Statevector.from_instruction(circuit(theta))
    return state.expectation_value([[1, 0], [0, -1]]).real
```

これが：

ハイブリッド計算の最小例

です。

---

# 古典最適化との連携

最適化には：

scipy

を使えます。

例：

```python
from scipy.optimize import minimize

result = minimize(expectation, x0=0)
```

これで：

最適なθ

が求まります。

---

# なぜ変分アルゴリズムが重要なのか？

理由：

浅い回路で動く

からです。

つまり：

ノイズに強い

という特徴があります。

これが：

NISQ時代に適している理由です。

---

# 勾配ベース最適化

パラメータ更新には：

勾配

を使う方法があります。

例：

gradient descent

これは：

量子機械学習

でも使われます。

---

# 勾配なし最適化

別の方法：

勾配なし最適化

例：

Nelder–Mead
COBYLA
SPSA

量子計算では：

こちらがよく使われます。

理由：

ノイズに強い

からです。

---

# SPSAとは何か？

SPSAは：

同時摂動確率近似法

です。

特徴：

少ない測定回数で

最適化できます。

そのため：

量子アルゴリズムに適しています。

---

# ハイブリッド計算の応用分野

応用例：

量子化学
材料設計
金融最適化
機械学習
グラフ最適化

つまり：

産業応用の中心

です。

---

# なぜ今重要なのか？

理由：

現在の量子コンピュータで

実行可能

だからです。

将来ではなく：

今

使えるアルゴリズムです。

---

# 将来の量子アルゴリズムとの関係

将来：

誤り訂正量子コンピュータ

が実現すると：

Shor

のようなアルゴリズムが主役になります。

しかし：

現在は：

変分アルゴリズム

が中心です。

---

# ハイブリッド計算の設計パターン

基本構造：

初期化
回路実行
測定
評価
更新

この繰り返しです。

これは：

機械学習

とよく似ています。

---

# まとめ

今回は：

* ハイブリッド量子古典計算
* 変分量子回路
* 勾配最適化
* SPSA
* 応用分野

を扱いました。

ここまでで：

NISQ時代の量子計算の中心モデル

を理解できました。

次回は：

量子機械学習の入口

を扱いながら：

量子回路とAIの関係

を理解していきます。
